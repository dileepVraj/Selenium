package apachePOI;

public class Terminologies
{
    /*WorkBook: work book is entire Excel book that contains multiple sheets where we can name them.
    *
    * Sheet: Sheets are part of workbook which replicates papers in book.
    *
    * Rows: We all know that each sheet in Excel workbook contains multiple rows.
    *
    * Columns: Each row in Excel sheet contains multiple columns.
    *
    * */

    /*We have two versions of Excel files which older versions of Excel files are stored with 'Xls' and newer
    * Excel files are stored with extension of 'Xlsx' version.
    *
    * These two versions are supported by POM API.*/
}
