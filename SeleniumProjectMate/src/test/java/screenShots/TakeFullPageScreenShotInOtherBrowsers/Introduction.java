package screenShots.TakeFullPageScreenShotInOtherBrowsers;

public class Introduction
{
    /**
     * we can capture screenshot of full webpage in other browsers using Ashot API.
     *
     * It is a Third part library.
     *
     * We have to add dependencies of Ashot to pom.xml file in the project by visiting the MVN repository.
     */

    public static void captureFullScreenShotUsingAshot()
    {
        /*ScreenShot screenshot = new Ashot().
        * shootingStrategy(ShootingStrategies.viewportPasting(1000).takeScreenShot(driver);
        *
        * File destinationPath = System.getProperty("user.dir")+//folderName//fileName.png;
        * ImageIO.write(screenshot.getImage(),"png",new File(destinationPath));*/

        /**
         * simulate the above code to capture the screenshot of full webpage in all other browsers except firefoxbrowser.
         *
         * before writing the above code add Ashot API dependency to pom.xml file.
         */


    }
}
