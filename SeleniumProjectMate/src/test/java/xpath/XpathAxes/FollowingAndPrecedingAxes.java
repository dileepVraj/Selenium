package xpath.XpathAxes;

public class FollowingAndPrecedingAxes
{
    /**
     * If we want to find all following elements after current element we have to use following axes which gives
     * all following elements of current element we have to get our required element using index concept here.
     *
     * same applies with preceding axes, it gives all ancestor elements of current element we have to use index concept
     * to identify specific element from all preceding elements.
     */
}
