package webTables;

public class Introduction
{
    /*
    Web Tables are used to organize similar types of content on the web page,
    Web tables are groups of elements that are logically stored in a row and column format.
    Web table has various HTML tags like., table, th, tr, td. Let’s understand these tags a bit more:

        <table> – It defines a table
        <th> – It defines a header cell
        <tr> – It defines a row in a table.
        <td>- It defines a cell in a table. the <td> tag always lies inside the <tr> tag.
        */
}
